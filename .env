# FaxJet RevenueCat — iOS public SDK key (project-isolated from Prevena).
# Filled during RevenueCat provisioning. Empty → RevenueCat disabled gracefully.
# Safe to commit (publishable SDK key); the v2 secret key never lives here.
EXPO_PUBLIC_RC_PUBLIC_KEY=
